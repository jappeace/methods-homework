\documentclass{article}
\usepackage{txfonts}
\usepackage{booktabs}
\usepackage{color}
\usepackage{bussproofs}
\usepackage{graphicx}
\usepackage{pifont}
\newenvironment{scprooftree}[1]%
{\gdef\scalefactor{#1}\begin{center}\proofSkipAmount \leavevmode}%
{\scalebox{\scalefactor}{\DisplayProof}\proofSkipAmount \end{center} }

\begin{document}
\author{Jappie Klooster}
\title{Excersizes of Linear Logic}
\maketitle
\section{Commutative conversions}
\begin{center}
\mbox{\AxiomC{$
	\Gamma \vdash A \times B
$}
\AxiomC{$
	\Delta, Q, Q, A, B \vdash C
$}
\RightLabel{$E\times$}
\BinaryInfC{$
	\Gamma, \Delta, Q, Q \vdash C
$}
\RightLabel{Cont}
\UnaryInfC{$
	\Gamma, \Delta, Q \vdash C
$}
\DisplayProof
}
$\Leftrightarrow$
\mbox{\AxiomC{$
	\Gamma \vdash A \times B
$}
\AxiomC{$
	\Delta, Q, Q, A, B \vdash C
$}
\RightLabel{Cont}
\UnaryInfC{$
	\Delta, Q, A, B \vdash C
$}
\RightLabel{$E\times$}
\BinaryInfC{$
	\Gamma, \Delta, Q \vdash C
$}

\DisplayProof
}
\end{center}
\begin{center}
\mbox{\AxiomC{$
	\Gamma, Q, Q \vdash A \times B
$}
\AxiomC{$
	\Delta, A, B \vdash C
$}
\RightLabel{$E\times$}
\BinaryInfC{$
	\Gamma, \Delta, Q, Q \vdash C
$}
\RightLabel{Cont}
\UnaryInfC{$
	\Gamma, \Delta, Q \vdash C
$}
\DisplayProof
}
$\Leftrightarrow$
\mbox{\AxiomC{$
	\Gamma, Q, Q \vdash A \times B
$}
\RightLabel{Cont}
\UnaryInfC{$
	\Gamma, Q \vdash A \times B
$}
\AxiomC{$
	\Delta, A, B \vdash C
$}
\RightLabel{$E\times$}
\BinaryInfC{$
	\Gamma, \Delta, Q \vdash C
$}

\DisplayProof
}
\end{center}
\subsection{Where can cont not be pushed up?}
If the Q is being split up on both sides of the proof, for example:
\begin{prooftree}

\AxiomC{$
	\Gamma, Q \vdash A \times B
$}
\AxiomC{$
	\Delta, Q, A, B \vdash C
$}
\RightLabel{$E\times$}
\BinaryInfC{$
	\Gamma, \Delta, Q, Q \vdash C
$}
\RightLabel{Cont}
\UnaryInfC{$
	\Gamma, \Delta, Q \vdash C
$}
\end{prooftree}

Now cont can't be pushed up because one side would have nothing to cont.

\section{Translate to ILL and show the translation is valid in ILL}
$\Gamma \Rightarrow A$ is proofable in IL iff its translation
${!}\Gamma^* \Rightarrow A^*$ is proofable in ILL

Translations

\begin{scprooftree}{0.75}
\AxiomC{$
$}
\RightLabel{$Ax$}
\UnaryInfC{$
	A^*
	\Rightarrow
	A^*
$}
\RightLabel{$L\&$}
\UnaryInfC{$
	A^* \& ( {!}B^* \oplus {!}C^*)
	\Rightarrow
	A^*
$}
\RightLabel{$L{!}$}
\UnaryInfC{$
	A^* \& ( {!}B^* \oplus {!}C^*)
	\Rightarrow
	A^*
$}
\RightLabel{$W{!}$}
\UnaryInfC{$
	{!}(A^* \& ( {!}B^* \oplus {!}C^*))
	\Rightarrow
	A^*
$}
\RightLabel{$W{!}$}
\UnaryInfC{$
	{!}(A^* \& ( {!}B^* \oplus {!}C^*)),
	{!}B^*
	\Rightarrow
	A^*
$}
\AxiomC{$
$}
\RightLabel{$Ax$}
\UnaryInfC{$
	B^*
	\Rightarrow
	B^*
$}
\RightLabel{$L{!}$}
\UnaryInfC{$
	{!}B^*
	\Rightarrow
	B^*
$}
\RightLabel{$W{!}$}
\UnaryInfC{$
	{!}(A^* \& ( {!}B^* \oplus {!}C^*)),
	{!}B^*
	\Rightarrow
	B^*
$}
\RightLabel{$R\&$}
\BinaryInfC{$
	{!}(A^* \& ( {!}B^* \oplus {!}C^*)),
	{!}B^*
	\Rightarrow
	A^* \& B^*
$}
\RightLabel{$R{!}$}
\UnaryInfC{$
	{!}(A^* \& ( {!}B^* \oplus {!}C^*)),
	{!}B^*
	\Rightarrow
	{!}(A^* \& B^*)
$}
\RightLabel{$R\oplus$}
\UnaryInfC{$
	{!}(A^* \& ( {!}B^* \oplus {!}C^*)),
	{!}B^*
	\Rightarrow
	{!}(A^* \& B^*) \oplus {!}(A^* \& C^*)
$}
\AxiomC{$
$}
\RightLabel{$Ax$}
\UnaryInfC{$
	A^*
	\Rightarrow
	A^*
$}
\RightLabel{$L\&$}
\UnaryInfC{$
	A^* \& ( {!}B^* \oplus {!}C^*)
	\Rightarrow
	A^*
$}
\RightLabel{$L{!}$}
\UnaryInfC{$
	A^* \& ( {!}B^* \oplus {!}C^*)
	\Rightarrow
	A^*
$}
\RightLabel{$W{!}$}
\UnaryInfC{$
	{!}(A^* \& ( {!}B^* \oplus {!}C^*))
	\Rightarrow
	A^*
$}
\RightLabel{$W{!}$}
\UnaryInfC{$
	{!}(A^* \& ( {!}B^* \oplus {!}C^*)),
	{!}C^*
	\Rightarrow
	A^*
$}
\AxiomC{$
$}
\RightLabel{$Ax$}
\UnaryInfC{$
	C^*
	\Rightarrow
	C^*
$}
\RightLabel{$L{!}$}
\UnaryInfC{$
	{!}C^*
	\Rightarrow
	C^*
$}
\RightLabel{$W{!}$}
\UnaryInfC{$
	{!}(A^* \& ( {!}B^* \oplus {!}C^*)),
	{!}C^*
	\Rightarrow
	C^*
$}
\RightLabel{$R\&$}
\BinaryInfC{$
	{!}(A^* \& ( {!}B^* \oplus {!}C^*)),
	{!}C^*
	\Rightarrow
	A^* \& C^*
$}
\RightLabel{$R{!}$}
\UnaryInfC{$
	{!}(A^* \& ( {!}B^* \oplus {!}C^*)),
	{!}C^*
	\Rightarrow
	{!}(A^* \& C^*)
$}
\RightLabel{$R\oplus$}
\UnaryInfC{$
	{!}(A^* \& ( {!}B^* \oplus {!}C^*)),
	{!}C^*
	\Rightarrow
	{!}(A^* \& B^*) \oplus {!}(A^* \& C^*)
$}
\RightLabel{$L\oplus$}
\BinaryInfC{$
	{!}(A^* \& ( {!}B^* \oplus {!}C^*)),
	{!}B^* \oplus {!}C^*
	\Rightarrow
	{!}(A^* \& B^*) \oplus {!}(A^* \& C^*)
$}
\RightLabel{$L\&$}
\UnaryInfC{$
	{!}(A^* \& ( {!}B^* \oplus {!}C^*)),
	A^* \& ( {!}B^* \oplus {!}C^*)
	\Rightarrow
	{!}(A^* \& B^*) \oplus {!}(A^* \& C^*)
$}
\RightLabel{$L{!}$}
\UnaryInfC{$
	{!}(A^* \& ( {!}B^* \oplus {!}C^*)),
	{!}(A^* \& ( {!}B^* \oplus {!}C^*))
	\Rightarrow
	{!}(A^* \& B^*) \oplus {!}(A^* \& C^*)
$}
\RightLabel{$C{!}$}
\UnaryInfC{$
	{!}(A^* \& ( {!}B^* \oplus {!}C^*))
	\Rightarrow
	{!}(A^* \& B^*) \oplus {!}(A^* \& C^*)
$}
\RightLabel{$(A + B)^* = {!}A^* \oplus {!}B^*$}
\UnaryInfC{$
	{!}(A^* \& ( B + C)^*)
	\Rightarrow
	{!}(A^* \& B^*) \oplus {!}(A^* \& C^*)
$}
\RightLabel{$(A \times B)^* = A^* \& B^*$}
\UnaryInfC{$
	{!}(A \times ( B + C ))^*
	\Rightarrow
	{!}( A \times B )^* \oplus {!}(A \times C )^*
$}
\RightLabel{$(A + B)^* = {!}A^* \oplus {!}B^*$}
\UnaryInfC{$
	{!}(A \times ( B + C ))^*
	\Rightarrow
	(( A \times B ) + ( A \times C ))^*
$}
\RightLabel{IL to ILL}
\UnaryInfC{$
	A \times ( B + C )
	\Rightarrow
	( A \times B ) + ( A \times C )
$}
\end{scprooftree}

\begin{scprooftree}{1}
\AxiomC{$
$}
\RightLabel{$Ax$}
\UnaryInfC{$
	A^*
	\Rightarrow
	A^*
$}
\RightLabel{$L\&$}
\UnaryInfC{$
	A^* \& B^*
	\Rightarrow
	A^*
$}
\RightLabel{${!}L$}
\UnaryInfC{$
	{!}( A^* \& B^* )
	\Rightarrow
	A^*
$}
\AxiomC{$
$}
\RightLabel{$Ax$}
\UnaryInfC{$
	A^*
	\Rightarrow
	A^*
$}
\RightLabel{$L\&$}
\UnaryInfC{$
	A^* \& C^*
	\Rightarrow
	A^*
$}
\RightLabel{$L{!}$}
\UnaryInfC{$
	{!}(A^* \& C^* )
	\Rightarrow
	A^*
$}
\RightLabel{$L\oplus$}
\BinaryInfC{$
	{!}( A^* \& B^* ) \oplus {!}( A^* \& C^* )
	\Rightarrow
	A^*
$}
\RightLabel{${!}L$}
\UnaryInfC{$
	{!}( {!}( A^* \& B^* ) \oplus {!}( A^* \& C^* ))
	\Rightarrow
	A^*
$}
\AxiomC{$
$}
\RightLabel{$Ax$}
\UnaryInfC{$
	B^*
	\Rightarrow
	B^*
$}
\RightLabel{$L\&$}
\UnaryInfC{$
	A^* \& B^*
	\Rightarrow
	B^*
$}
\RightLabel{$L{!}$}
\UnaryInfC{$
	{!}( A^* \& B^* )
	\Rightarrow
	B^*
$}
\RightLabel{$R{!}$}
\UnaryInfC{$
	{!}( A^* \& B^* )
	\Rightarrow
	{!}B^*
$}
\RightLabel{$R\oplus$}
\UnaryInfC{$
	{!}( A^* \& B^* )
	\Rightarrow
	{!}B^* \oplus {!}C^*
$}
\AxiomC{$
$}
\RightLabel{$Ax$}
\UnaryInfC{$
	C^*
	\Rightarrow
	C^*
$}
\RightLabel{$L\&$}
\UnaryInfC{$
	A^* \& C^*
	\Rightarrow
	C^*
$}
\RightLabel{$L{!}$}
\UnaryInfC{$
	{!}( A^* \& C^* )
	\Rightarrow
	C^*
$}
\RightLabel{$R{!}$}
\UnaryInfC{$
	{!}( A^* \& C^* )
	\Rightarrow
	{!}C^*
$}
\RightLabel{$R\oplus$}
\UnaryInfC{$
	{!}( A^* \& C^* )
	\Rightarrow
	{!}B^* \oplus {!}C^*
$}
\RightLabel{$L\oplus$}
\BinaryInfC{$
	{!}( A^* \& B^* ) \oplus {!}( A^* \& C^* )
	\Rightarrow
	{!}B^* \oplus {!}C^*
$}
\RightLabel{$R{!}$}
\UnaryInfC{$
	{!}( {!}( A^* \& B^* ) \oplus {!}( A^* \& C^* ))
	\Rightarrow
	{!}B^* \oplus {!}C^*
$}
\RightLabel{$R\&$}
\BinaryInfC{$
	{!}( {!}( A^* \& B^* ) \oplus {!}( A^* \& C^* ))
	\Rightarrow
	A^* \& ( {!}B^* \oplus {!}C^* )
$}
\RightLabel{$(A + B)^* = {!}A^* \oplus {!}B^*$}
\UnaryInfC{$
	{!}( {!}( A^* \& B^* ) \oplus {!}( A^* \& C^* ))
	\Rightarrow
	A^* \& ( B + C )^*
$}
\RightLabel{$(A \times B)^* = A^* \& B^*$}
\UnaryInfC{$
	{!}(( A \times B )^* \oplus ( A \times C )^*)
	\Rightarrow
	(A \times ( B + C ))^*
$}
\RightLabel{$(A + B)^* = {!}A^* \oplus {!}B^*$}
\UnaryInfC{$
	{!}(( A \times B ) + ( A \times C ))^*
	\Rightarrow
	(A \times ( B + C ))^*
$}
\RightLabel{IL to ILL}
\UnaryInfC{$
	( A \times B ) + ( A \times C )
	\Rightarrow
	A \times ( B + C )
$}
\end{scprooftree}

\section{No noun phrase}
So for both 3 and 4 I created one giant proof tree,
but even withthe forbidden rules I can't solve this one. \\

The controled rules are only limited to that, but I found
especially on the right side of the three that the positional
part can quickly become irrelevant if you have only 1 formula
(s in this case) that can move around. You can just swap positions
with other formulas, however I may have interpeted the rule a bit
to freely, it doesn't become clear from the symbols if you can
swap both sides and or skip whole parts. I assume it is because
the $\Delta$ stands for sequances.

\begin{tabular}{@{}ll@{}}
	word & type \\ \toprule
	the & $np/n$ \\
	book & $n$ \\
	that & $(n\setminus n) /(s / np)$ \\
	Allice & $np$ \\
	read & $(np \setminus s) / np$ \\
	yesterday & $s \setminus s$ \\
\end{tabular}

\begin{prooftree}
	\AxiomC{}
	\UnaryInfC{$
		np \Rightarrow np
	$}
	\AxiomC{}
	\UnaryInfC{$
		s \Rightarrow s
	$}
	\AxiomC{??}
	\UnaryInfC{$
		s \Rightarrow np
	$}
	\AxiomC{??}
	\UnaryInfC{$
		s \Rightarrow np
	$}
	\RightLabel{$L\setminus$}
	\BinaryInfC{$
		s, np \setminus s \Rightarrow np
	$}
	\RightLabel{$L\setminus$}
	\BinaryInfC{$
		s, s \setminus s, np \setminus s
		\Rightarrow
		np
	$}
	\RightLabel{$C^*$}
	\UnaryInfC{$
		s \setminus s, s, np \setminus s
		\Rightarrow
		np
	$}
	\RightLabel{$L/$}
	\BinaryInfC{$
		s \setminus s, s, (np \setminus s) / np, np
		\Rightarrow
		np
	$}
	\RightLabel{$C^*$}
	\UnaryInfC{$
		s \setminus s, (np \setminus s) / np, s, np
		\Rightarrow
		np
	$}
	\RightLabel{$C^*$}
	\UnaryInfC{$
		s, (np \setminus s) / np, s \setminus s, np
		\Rightarrow
		np
	$}
	\RightLabel{$C^*$}
	\UnaryInfC{$
		np, (np \setminus s) / np, s \setminus s, s
		\Rightarrow
		np
	$}
	\RightLabel{$R/$}
	\UnaryInfC{$
		np, (np \setminus s) / np, s \setminus s
		\Rightarrow
		s / np
	$}
	\AxiomC{}
	\UnaryInfC{$
		n \Rightarrow n
	$}
	\AxiomC{}
	\UnaryInfC{$
		n \Rightarrow n
	$}
	\AxiomC{}
	\UnaryInfC{$
		np \Rightarrow np
	$}
	\RightLabel{$L/$}
	\BinaryInfC{$
		np / n, n
		\Rightarrow
		np
	$}
	\RightLabel{$L\setminus$}
	\BinaryInfC{$
		np / n,
		[
		n,
		n \setminus n
		]
		\Rightarrow
		np
	$}
	\RightLabel{$A^*$}
	\UnaryInfC{$
		np / n,
		n,
		[
		n \setminus n
		]
		\Rightarrow
		np
	$}
	\RightLabel{$L/$}
	\BinaryInfC{$
		np/n, n,
		[
		(n \setminus n) / ( s / np),
		np, (np \setminus s) / np, s \setminus s
		]
		\Rightarrow
		np
	$}
	\RightLabel{Words to types}
	\UnaryInfC{The book that allice read yesterday.
		$
		\Rightarrow
		np
		$
	}
\end{prooftree}
\end{document}
